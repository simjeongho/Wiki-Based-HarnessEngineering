# wiki-lint e2e verify run — 2026-06-29 (스냅샷 전용, 다음 커밋에서 원복)

> plan Task 3 Step 2의 end-to-end verify 산출물. fixture wiki 그래프(모순 1·orphan 1·출처누락 1을
> 포함하되 okf-lint는 통과)를 실제 `wiki/`에 두고 `wiki-lint`를 라이브 1회 실행한 기록.
> **이 디렉터리와 fixture·리포트는 원복 커밋에서 작업트리에서 제거된다(히스토리에만 보존).**

## fixture 그래프 (okf-lint: OK 선확인)
- `wiki/capabilities/payment.md` — SPINE 허브, 본문에서 `[[entities/order]]`·`[[entities/member]]` 링크
- `wiki/entities/order.md` — status enum `{pending,paid,shipped,cancelled}`, `related: [[policies/refund-rule]]`
- `wiki/policies/refund-rule.md` — 환불 시 status를 `refunded`로 전이(=order enum에 없는 값 → 모순)
- `wiki/entities/member.md` — `sources: raw/data-models/member.md` (이 raw 파일은 의도적으로 미생성 → dangling). payment가 링크 → orphan 아님
- `wiki/queries/legacy-report.md` — 아무도 링크 안 함, type=query → 진짜 orphan
- raw: payment/order/refund-rule/legacy-report 출처는 생성(존재), member 출처는 미생성(dangling)

## 라이브 실행 결과 (reports/wiki-lint/2026-06-29.md)
```
# wiki-lint 리포트 — 2026-06-29
## 요약: 모순 1 · orphan 1 · 출처누락 1

## 모순 (contradiction)
- [[entities/order]] ↔ [[policies/refund-rule]]
  `entities/order`의 status enum에 `refunded`가 없으나, `policies/refund-rule`은 환불 완료 시 status를 `refunded`로 전이한다고 명시함.
  근거: `entities/order` — "허용 enum 값: `pending`, `paid`, `shipped`, `cancelled`." ↔ `policies/refund-rule` — "환불 완료 시 주문 status를 `refunded`로 전이한다."

## orphan
- [[queries/legacy-report]] — 인바운드 0, type=query(SPINE 허브 아님), index.md에서도 링크 없음 → 고립

## 출처 누락 (source integrity)
- [[entities/member]] — sources[0].path=raw/data-models/member.md 디스크에 없음

## 처리 안내
다음날 사람/AI가 /ingest(반영) 또는 operate 루프로 처리. wiki-lint는 비차단 진단만.
```

## 검증 포인트
- 모순 1: order↔refund-rule (근거 인용 포함) — FA2.
- orphan 1: legacy-report만. payment(type=capability 허브)·member(payment로부터 인바운드 1) 정확히 제외 — FA4.
- 출처누락 1: member dangling만. order/refund-rule/payment/legacy-report 출처는 존재 → 오보 없음 — FA5.
- 쓰기 대상은 reports/ 하나뿐, wiki/raw/index/log 불변 — FA6.
- 흐름: 그래프 구성(본문+frontmatter 엣지) → 3탐지기 → 리포트 → 비차단. okf-lint-clean 번들 위에서 의미 계층만 진단.
