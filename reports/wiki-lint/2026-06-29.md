# wiki-lint 리포트 — 2026-06-29
## 요약: 모순 1 · orphan 1 · 출처누락 1

## 모순 (contradiction)
- [[entities/order]] ↔ [[policies/refund-rule]]
  `entities/order`의 status enum에 `refunded`가 없으나, `policies/refund-rule`은 환불 완료 시 status를 `refunded`로 전이한다고 명시함.
  근거: `entities/order` — "허용 enum 값: `pending`, `paid`, `shipped`, `cancelled`." ↔ `policies/refund-rule` — "환불 완료 시 주문 status를 `refunded`로 전이한다."

## orphan
- [[queries/legacy-report]] — 인바운드 0, type=query(SPINE 허브 아님), index.md에서도 링크 없음 → 고립

## 출처 누락 (source integrity)
- [[entities/member]] — sources[0].path=raw/data-models/member.md 디스크에 없음

## 처리 안내
다음날 사람/AI가 /ingest(반영) 또는 operate 루프로 처리. wiki-lint는 비차단 진단만.
